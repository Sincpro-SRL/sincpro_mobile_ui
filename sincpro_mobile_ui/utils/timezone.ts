/**
 * @deprecated Region-coupled (LatAm locale data); relocated to the core alongside the
 * TimeZone widgets. Import from `@sincpro/mobile/ui/components/molecules` instead. Kept
 * here for backwards compatibility and removed in the next major.
 */
export interface TimezoneLocale {
  timezone: string;
  locale: string;
}
